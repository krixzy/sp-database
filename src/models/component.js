export default class Component {
    constructor(length, width, height, price) {
        this.length = length;
        this.width = width;
        this.height = height;
        this.price = price;
    }

}